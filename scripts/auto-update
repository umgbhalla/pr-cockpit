#!/usr/bin/env bash
# Non-graphical updater: stage and atomically activate without launching Electron.
set -Eeuo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec env COCKPIT_AUTO_UPDATE=1 COCKPIT_UPDATE_GUI=0 "$root/scripts/update"
